<h1>[{{title}}] 글{{mode}}작성{{else}}</h1>
<div>
  <form name="boardForm" action="/write" method="POST">
    <div>
      <label>제목</label>
      <input
        type="text"
        name="title"
        placeholder="제목을 입력하세요"
        value="{{post.title}}"
      />
    </div>
    <div>
      <label>이름</label>
      <input
        type="text"
        name="writer"
        placeholder="이름을 입력하세요"
        value="{{post.author}}"
      />
    </div>
    <div>
      <label>비밀번호</label>
      <input
        type="password"
        name="password"
        placeholder="비밀번호를 입력하세요"
        value="{{post.password}}"
      />
    </div>
    <div>
      <label>본문</label>
      <textarea name="content" placeholder="본문을 입력하세요"></textarea>
    </div>
    <button type="submit">저장</button>
    <button type="reset">초기화</button>
  </form>
</div>
